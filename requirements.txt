# Core video processing
moviepy>=1.0.3
numpy>=1.24.0

# Speech-to-text (transcription + captions)
openai-whisper>=20231117

# Scene detection
scenedetect[opencv]>=0.6.2
opencv-python-headless>=4.8.0

# Summarization for header generation (optional — falls back to extractive)
transformers>=4.35.0
torch>=2.0.0

# Audio processing
librosa>=0.10.0

# Utilities
tqdm>=4.65.0
