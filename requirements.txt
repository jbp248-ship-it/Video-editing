# Core video processing
moviepy>=1.0.3
numpy>=1.24.0

# Speech-to-text (transcription + captions)
openai-whisper>=20231117

# Scene detection
scenedetect[opencv]>=0.6.2
opencv-python-headless>=4.8.0

# Local AI models (downloaded automatically on first use via HuggingFace)
# - facebook/bart-large-cnn (~1.6GB) for summarization — no API key needed
transformers>=4.35.0
torch>=2.0.0

# Audio processing
librosa>=0.10.0

# Web app
flask>=3.0.0

# Utilities
tqdm>=4.65.0

# Build & packaging (optional — only needed for creating .exe distributable)
# pyinstaller>=6.0.0
